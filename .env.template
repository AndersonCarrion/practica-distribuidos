# === Configuración específica de esta máquina ===
# Copiar este archivo como .env y ajustar los valores

NODE_NAME=Nodo-A

JWT_SECRET=cambia_este_secreto_por_uno_seguro

# === IPs de las 3 máquinas del cluster (igual en todas) ===
# Usar ipconfig para obtener la IP LAN de cada PC
# Si solo hay 2 PCs, dejar MAQUINA3_IP vacío o comentado
MAQUINA1_IP=192.168.1.10
MAQUINA2_IP=192.168.1.20
MAQUINA3_IP=192.168.1.30

# === Inicializar replica set (1 vez) ===
# docker-compose up --build -d
# .\scripts\init-replicaset.ps1    # Windows
# docker-compose exec mongo bash /scripts/init-replicaset.sh   # Linux
